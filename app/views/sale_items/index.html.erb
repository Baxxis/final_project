<p id="notice"><%= notice %></p>

<h1>Sale Items</h1>

<table>
  <thead>
    <tr>
      <th>Percent discount</th>
      <th>Begin date</th>
      <th>End date</th>
      <th>Product</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sale_items.each do |sale_item| %>
      <tr>
        <td><%= sale_item.percent_discount %></td>
        <td><%= sale_item.begin_date %></td>
        <td><%= sale_item.end_date %></td>
        <td><%= sale_item.product %></td>
        <td><%= link_to 'Show', sale_item %></td>
        <td><%= link_to 'Edit', edit_sale_item_path(sale_item) %></td>
        <td><%= link_to 'Destroy', sale_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sale Item', new_sale_item_path %>
